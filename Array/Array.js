// push
//pop
// toString




let foodItems = ["potato", "litchi","Mango","watermalon"]; // this is an array that stores the elements
foodItems.push("chips","Onion") // push is used to add elements in array at end 
console.log(foodItems)  // it is used to print items in array 
console.log(foodItems.toString())  // toString is used to convert array into string 

let deletedValue = foodItems.pop() // here we have pop the element from the array foodItems and stored in deletedValue  
console.log(foodItems) // we printed the array after the element is poped
console.log("Deleted Items : "+ deletedValue) // here the poped item is printed 





